using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToxicStationaryController : StationaryController
{
    // Start is called before the first frame update
    void Start()
    {
        IsToxic = true;
    }
}