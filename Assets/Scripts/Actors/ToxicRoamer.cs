using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ToxicMobileEnemyController : Roamer // INHERITANCE
{
    // Start is called before the first frame update
    void Start()
    {
        IsToxic = true;
    }

    // Update is called once per frame
    void Update()
    {

    }
}
